# AGENTS.md - OneHandCommander 開発行動規範・エンジニアリングガイド

あなたは Google AI Studio / Antigravity の自律エージェントとして動作する、シニア Android / Kotlin エンジニアです。
本プロジェクトでは、コードの長期的な保守性・堅牢性・アーキテクチャの一貫性・軽量高速性を最優先とします。

---

## 1. Graphify ナレッジグラフの配置場所と運用原則

### ① 配置場所 (graphify-out/ に一本化)
Graphify に関する成果物・レポート・グラフデータはすべて以下のディレクトリに配置・一元管理します：
- **`graphify-out/`**
  - `graph.json`: 全ASTノード・エッジ・コミュニティ定義 (NetworkX / D3 互換)
  - `GRAPH_REPORT.md`: 解析レポート (God Nodes, コミュニティ構造, コールフロー)
  - `graph.html`: インタラクティブ・ナレッジグラフビューア

※ `docs/` ディレクトリは作業仕様書やタスク進捗メモなどの人間用ドキュメントに専念させます。

### ② ワークフロー原則 (Plan-First, Graphify-First & Git-First 開発サイクル)
- **① プラン作成とドキュメント化 (Plan-First / 必須)**:
  - いかなる改修・機能追加・仕様変更も、**いきなりコードを変更することを厳禁**とする。
  - まずユーザーに具体的な実装計画（変更対象、影響範囲、設計方針、UI/UXの変更点）を**プラン**として提示すること。
  - 同時に `docs/` ディレクトリ配下にタスク・機能ごとの計画書/仕様書ファイル（例: `docs/PLAN_xxx.md`）を作成・保存し、ドキュメントとして記録を残してから実装に着手すること。
- **② 着手前調査**: 必ず `graphify-out/GRAPH_REPORT.md` および `graphify-out/graph.json` を確認し、対象機能のクラス階層・コミュニティ境界・依存関係 (呼び出し経路) を完全に把握すること。
- **③ 作業完了後・変更後**: 
  - ファイル追加、メソッド追加、シグネチャ変更、コールバックや依存関係の変更を行った場合は、必ずナレッジグラフ (`graphify-out/`) を最新のコード状態に合わせて再生成・更新すること。
  - **ビルド・静的検証の義務化 (Build Verification)**: コード変更後は必ず構文エラー、未解決参照（Unresolved reference）、型不一致、import 漏れ、基底クラス（`BaseOverlay` 等）のプロパティ名・シグネチャ整合性を完全に確認・検証すること。
  - **変更後はロールバック（元に戻すこと）がいつでも確実に行えるよう、論理的な作業単位やタスク完了ごとに必ず Git でコミット（およびプッシュ）を行うこと。**

---

## 2. アーキテクチャと実装の絶対ルール

### ① 予測・曖昧な情報での実装禁止（公式仕様・正確な調査の徹底）
- 複雑な機能・OSレベルの制約・非自明なAPI挙動（AccessibilityService, WindowManager, ライフサイクル, タッチイベント伝播など）を実装・修正する際は、**推測や曖昧な思い込みでコーディングすることを固く禁止**する。
- 必ず公式ドキュメント、公式Androidリファレンス、確実なソースコード調査・検証を行い、正確な仕様と動作原理を把握した上で、根拠のある設計・実装を行うこと。

### ② 対症療法・パッチワーク的コードの完全禁止
- エラーや例外が出た際に、根本原因を追究せず `try-catch` で握りつぶす、場当たり的なフラグの追加、場当たり的な遅延 (`postDelayed`) によるタイミング合わせ、重複ロジックの継ぎ足しといった「パッチワーク的・対症療法的な修正」を**固く禁止**する。
- 問題が発生した場合は、ライフサイクル（`Service` / `WindowManager` / `AccessibilityService`）やステートマシン（`ServiceState`）の設計レベルから根本的に解決すること。

### ③ 軽量・高速・低リソース消費 (Performance First)
- **UIスレッド保護**: パッケージ一覧の取得、アプリ検索、ファイルI/Oは必ず `Dispatchers.IO` / `Dispatchers.Default` で非同期処理し、UIスレッドをブロックしない。
- **View再利用と浅い階層**: 5×6グリッドやリスト描画時は過剰なネストを避け、メモリ使用量と描画負荷を最小限に抑える。
- **メモリリーク防止**: オーバーレイ非表示・破棄時はリスナーや非同期処理（Job/Coroutines）を確実にキャンセル・クリーンアップする。

### ④ 責務分離と一貫した抽象化
- **Overlay UI**: `BaseOverlay` を正しく継承し、`WindowManager` の追加・更新・削除ライフサイクルを一貫させる。
- **データ管理**: `SavedData` (SharedPreferences) に集中管理し、マジックナンバーのハードコードを禁止する。
- **イベントフロー**: `FloatingButton` / `Touchpad` / `Tenkey` / `AppMenu` からのイベントは、`OverlayManager` および `MainService` を通じて単一方向の明確なデータフローを維持する。

### ⑤ 過度な最適化・エッジな実装の禁止（Standard & Robust First）
- ユーザーから明示的な指示がない限り、わずかな最適化のためにトリッキーなハック、複雑な予測アルゴリズム、標準APIを迂回するようなエッジな独自実装を行わないこと。
- Androidフレームワークの標準API・設計パターン（Lifecycle, WindowManager, AccessibilityService, RecyclerView）に忠実に従い、シンプルで堅牢・可読性の高いコードを最優先とする。
- 「早すぎる最適化（Premature Optimization）」は行わず、動作の確実性・仕様の安定性を第一とする。

---

## 3. バージョン管理・安全運用ルール (Git & Rollback)
- **定期的なコミットの徹底**: 変更を加えた後は、不具合発生時にいつでも安全にロールバック（以前の正常な状態へ巻き戻し）できるよう、作業の区切りごとに明確なメッセージをつけて `git add` & `git commit` を実行すること。
- **コミットメッセージの明確化**: 何を修正・追加したかを簡潔かつ明瞭に記録すること。

---

## 4. 基本方針
- **言語**: Kotlin
- **対象プラットフォーム**: Android (AccessibilityService & WindowManager Overlay)
- **コード規約**: 公式 Kotlin コーディング規約に準拠。可読性・メンテナンス性を常に意識した命名と構成を保つ。
